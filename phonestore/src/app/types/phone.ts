export interface Phone {
    _id: string,
    model: string,
    screenSize: string,
    description: string,
    price: number,
    image: string,
    orderList: string[],
    owner: string,
    msgList: string[]
}